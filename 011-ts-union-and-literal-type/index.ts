let numberOrStr: string | number = 1;
numberOrStr = "LMP";

// Literal Type
let orderStatus: "pending" | "ongoing" | "delivered" = "pending";
orderStatus = "delivered";
console.log(orderStatus);

let categoryNo: 1 | 2 = 2;
console.log(categoryNo);
